import { Icon } from 'lucide-react';
import { footballHelmet } from '@lucide/lab';
import type { LucideProps } from 'lucide-react';

export const FootballHelmetIcon = (props: LucideProps) => (
  <Icon iconNode={footballHelmet} {...props} />
);
